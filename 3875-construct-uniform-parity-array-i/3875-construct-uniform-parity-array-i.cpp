class Solution {
public:
    bool uniformArray(vector<int>& nums) {
        // int mn = INT_MAX;

        // for (int x : nums)
        //     mn = min(mn, x);

        // if (mn & 1)
        //     return true;

        // for (int x : nums)
        //     if ((x & 1) != (nums[0] & 1))
        //         return false;

        return true;
    }
};